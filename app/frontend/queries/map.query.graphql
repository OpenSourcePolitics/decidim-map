query GetMap{
  map {
    proposals {
      id
      title
      address
      body
      coordinates {
        latitude
        longitude
      }
    }
    meetings {
      id
      title {
        translation(locale: "fr")
      }
      coordinates {
        latitude
        longitude
      }
    }
  }
}
